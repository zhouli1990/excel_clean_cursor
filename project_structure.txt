.
├── app.py                   # Flask 主应用程序文件
├── processor.py             # LLM 数据处理逻辑模块
├── feishu_utils.py          # 飞书 API 交互工具模块
├── postprocessor.py         # 后处理校验逻辑模块
├── diff_utils.py            # 数据差异比较工具模块
├── config.json              # 配置文件 (存储 LLM, 飞书等默认设置)
├── requirements.txt         # Python 依赖项列表 (需要手动生成)
├── .gitignore               # Git 忽略文件列表
├── templates/               # Flask 模板目录
│   └── index.html           # 前端 UI 页面
├── uploads/                 # [运行时生成] 上传文件存储目录 (按 task_id 分隔)
│   └── [task_id]/           #   - 任务对应的上传文件
│       └── ...
├── outputs/                 # [运行时生成] 处理结果输出目录 (按 task_id 分隔)
│   └── [task_id]/           #   - 任务对应的输出文件 (consolidated, merged, final, edited, diff...)
│       └── ...
└── README.md                # (建议添加) 项目说明文件

# 注意: uploads/ 和 outputs/ 目录及其内容是在程序运行时动态创建的。
# requirements.txt 需要根据实际使用的库手动生成 (例如: pip freeze > requirements.txt)。 